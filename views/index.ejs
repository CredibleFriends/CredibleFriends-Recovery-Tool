<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
		<title><%- page %></title>
		<!-- Bootstrap -->
		<!-- Latest compiled and minified CSS -->
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
		<link href="/stylesheets/style.css" rel="stylesheet">
		<!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
		<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
		<!--[if lt IE 9]>
		<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
		<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
		<![endif]-->
	</head>
	<body ng-app="CFRecoveryTool">
		<div class="container-fluid" ng-controller="MainController">
			<div class="rows">
				<div class="col-md-12">
					<h1 class="text-center">CF Recovery Tool</h1>
				</div>
			</div>
			<div class="rows" ng-cloak>
				<div class="alert alert-danger" role="alert" ng-if="recovery.error" ng-bind="recovery.message"></div>
				<form>
					<div class="form-group">
						<label>Recovery Phrase</label>
						<textarea ng-model="recovery.phrase" class="form-control" rows="5"></textarea>
					</div>
					<div class="form-group">
						<label>CF Public Key</label>
						<input type="text" ng-model="recovery.cf_public_key" class="form-control" />
					</div>
					<div class="form-group">
						<label>Address to send to</label>
						<input type="text" ng-model="recovery.addressToSend" class="form-control" />
					</div>
					<div class="form-group" >
						<label>Network</label>
						<select class="form-control" ng-model="recovery.network">
						  <option value="live">Livenet</option>
						  <option value="test">Testnet</option>
						</select>
					</div>
					<button ng-bind="recovery.caption" ng-disabled="recovery.process" type="button" ng-click="doRecovery()" class="btn btn-primary pull-right"></button>
				</form>
			</div>
		</div>
		<script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.5.3/angular.min.js"></script>
		<script src="/javascripts/app.js"></script>
	</body>
</html>